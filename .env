# This file should NOT be committed to version control.
# It is used for local development and testing to load environment variables.

# OpenWebUI / OpenAI-compatible endpoint credentials
OPENAI_BASE_URL="http://localhost:8080/v1"
OPENAI_API_KEY="sk-your-openwebui-api-key" # Or any dummy value if not strictly enforced by OpenWebUI
