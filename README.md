# CogniAgents Documentation

This directory contains the source files for the CogniAgents documentation, built with MkDocs.

## Prerequisites

To build and serve the documentation locally, you need to install the required packages. If you have already installed dev dependencies via `pip install -e ".[dev]"`, these are already installed.

